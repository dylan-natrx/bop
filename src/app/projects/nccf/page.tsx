/**
 * NCCF holding page. Scaffold only: proves the tenant routes, gates, and
 * renders with its own layout. Real content lands in a later phase.
 */
export default function NccfPage() {
  return (
    <main className="nccf-page">
      <p className="nccf-kicker">natrx.report platform</p>
      <h1 className="nccf-title">North Carolina Coastal Federation</h1>
      <p className="nccf-note">
        This project is being prepared. Content is on its way.
      </p>
    </main>
  )
}
